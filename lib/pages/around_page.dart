import 'package:flutter/material.dart';

class AroundPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('around'),),
      body: Center(
        child: Text('around'),
      ),
    );  
  }
}